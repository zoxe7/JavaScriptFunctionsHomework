Список задач:

1. Базовый вызов функции
2. Параметризированное приветствие
3. Приветствие с проверкой аргумента
4. Возведение в квадрат
5. Сумма элементов массива
6. Нахождение минимума из двух чисел
7. Стрелочная функция
8. Обработка колбэков
9. Фильтрация слов по длине
10. Преобразование массива строк в верхний регистр
11. Число в произвольной степени
12. Проверка на чётность
13. Округление числа вниз
14. Проверка нахождения числа в диапазоне
15. Повтор строки


Внимательно прочитал каждую задачу, чтобы понять, что требуется на вход и что нужно вернуть на выходе.
Если задача казалась сложной, я мысленно разбивал её на более мелкие, простые подзадачи.
Начал писать код, используя базовые конструкции JavaScript:
    *   function для определения функций.
    *   if...else для условных операторов.
    *   for для циклов.
    *   return для возврата значений.
    *   Методы массивов: filter(), map().
    *   Стрелочные функции (=>).
После написания каждой функции я тестировал её с разными входными данными, чтобы убедиться, что она работает правильно.  Я использовал console.log() для вывода результатов.
Использование встроенных функций:  Где это возможно, я использовал встроенные функции JavaScript, такие как Math.pow(), Math.floor(), чтобы сделать код более лаконичным и эффективным.
Я старался писать код так, чтобы он был легко читаемым и понятным.

  Не использовал очевидные имена переменных
  Сделал код максимально простым и понятным
